# Twirp Proto Tester Environment Configuration

# Server port (default: 8765)
PORT=8765

# Server host (default: 0.0.0.0 for all interfaces)
HOST=0.0.0.0

# Node environment (development, production)
NODE_ENV=production
